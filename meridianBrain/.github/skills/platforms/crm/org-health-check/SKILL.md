---
name: crm-org-health-check
description: Check Salesforce org roles and auth via crm-tools org-status and org-validate.
allowed-tools:
  - bash
metadata:
  author: Platform Engineering
  version: 1.0
---

> **Meridian:** Active — GitHub Copilot skill (`/.github/skills/`).

# CRM org health check

Run `crm-tools org-status` and `org-validate` with JSON output.
